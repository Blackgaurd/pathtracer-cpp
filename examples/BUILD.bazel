load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "cornell-box",
    srcs = ["cornell/script.cc"],
    copts = [
        "-std=c++17",
        "-O3",
    ],
    deps = ["//pathtracer"],
)
