load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "main",
    srcs = ["main.cc"],
    copts = ["-std=c++17"], # for unique_ptr
    deps = [
        "//raytracer:render",
        "//raytracer:object",
        "//raytracer:image",
        "//fpng:fpng",
    ],
)